var all_counter = 0;
var regex_counter = 0;
$("table").each(function() {
    all_counter++;
    var name =  $(this).prev().text();
    $(this).find("tr") .each(function(){
        var regex = $(this).find("td").text();
        if (regex.startsWith("Regex: ")){
            has_regex = 1;
            regex_counter++;
            console.log(name+":"+regex);    
            return false;
        }
    })
});
console.log(all_counter);
console.log(regex_counter);
